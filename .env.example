# Local development env example
# Use NEXT_PUBLIC_USE_SQLITE=true to enable the local SQLite auth flow
NEXT_PUBLIC_USE_SQLITE=true

# Session configuration
SESSION_TOKEN_SECRET=replace_this_with_a_strong_random_value
SESSION_COOKIE_NAME=session
SESSION_MAX_AGE=604800

# Supabase (optional) - set these if you want to use Supabase instead of SQLite
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Next.js
NODE_ENV=development
# Email Configuration for CTR Reactor Support System
# Copy this to .env.local and fill in your email settings

# Zoho Mail SMTP Configuration
SMTP_HOST=smtp.zoho.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=support@ctr-reactor.com
SMTP_PASS=your-zoho-mail-password

# Alternative: Use Resend (recommended for production)
# RESEND_API_KEY=your-resend-api-key

# Zoho Mail Setup Instructions:
# 1. Make sure support@ctr-reactor.com is set up in your Zoho Mail account
# 2. The password should be your Zoho Mail password (not your main Zoho password)
# 3. If using 2FA, you might need to generate an app-specific password
# 4. For SSL connection, you can also use port 465 with SMTP_SECURE=true

# For Vercel deployment, add these as environment variables in your Vercel dashboard

# Name.com Domain Search API
NAME_COM_API_KEY=your-name-com-api-key-here
NAME_COM_USERNAME=your-name-com-username-here