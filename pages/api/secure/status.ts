// Intentionally left blank: disabled pages route placeholder

// Disabled fallback: This file intentionally has no default export to avoid route duplication
// See `app/api/secure/status/route.ts` for App Router implementation.

export const disabled = true;

// Keeping previous handler content for reference, but not exporting it as default.
// Handler function removed to avoid duplicate app/pages router route.
// Use `app/api/secure/status/route.ts` for the actual implementation.
